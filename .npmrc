# LOCAL-ONLY (untracked): this box cannot reach registry.npmjs.org or the
# yarnpkg mirror (egress timeout); registry.npmmirror.com is reachable and fast.
# Do NOT commit this registry line — it is a workaround for THIS host's network.
registry=https://registry.npmmirror.com/
audit=false
fund=false
